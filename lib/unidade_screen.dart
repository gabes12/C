import 'package:flutter/material.dart';

class UnidadeScreen extends StatelessWidget {
  const UnidadeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      backgroundColor: Colors.white,
      body: Center(
        child: Text("Unidade"),
      ),
    );
  }
}
