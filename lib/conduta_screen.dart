import 'package:flutter/material.dart';

class CondutaScreen extends StatelessWidget {
  const CondutaScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      backgroundColor: Colors.white,
      body: Center(
        child: Text("Conduta"),
      ),
    );
  }
}
