import 'package:flutter/material.dart';

class CartaoFarmaciaScreen extends StatelessWidget {
  const CartaoFarmaciaScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      backgroundColor: Colors.white,
      body: Center(
        child: Text("Cartão de Farmácia"),
      ),
    );
  }
}
